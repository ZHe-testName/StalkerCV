# StalkerCV — память проекта

Агент: в начале чата прочитай этот файл целиком. После любого решения, которое меняет контракт, сцену, стек или план — обнови соответствующий раздел. Не пересказывай стенограмму, пиши факты.

Хозяин переезжает на другой комп: клонирует репу, открывает папку в Cursor, новый чат. Этот файл и есть восстановленный контекст.

## Что это

Не демка Three.js и не скин «киберпанк/Сталкер». Рекрутер за 20–40 секунд заходит в схрон. Комната — биография, которую осматривают, а не количество контента.

Вид от первого лица, **без WASD**. Кадр 0 — вся берлога из проёма (четвёртая стена открыта): сразу имя, роль, все якоря. Клик по подсвеченному предмету → камера сама подходит (~1 сек) → объект в кадре, справа/поверх текст. Отход — снова вся сцена. Глагол: осматривать чужое, не бегать. Документ нельзя прятать за квест: имя, роль и контакт видны с любого экрана.

## Три якоря, не больше

| Якорь | Предмет | Контент |
| --- | --- | --- |
| опыт / проекты | стол, ЭЛТ-монитор | клик по монитору |
| скилы | прибор на крышке серванта | оливковый бокс с янтарным экраном |
| контакт | КПК на дальнем подлокотнике кресла | зелёный экран |

Рабочий стул у стола — не якорь. Кресло у правой стены — якорь. Не путать.

На дальней стене, в промежутке между столом и сервантом, позже имя и профессия — в глаза с кадра 0.

## Стек — не менять без спроса

- Vue 3 + **Nuxt 4.5** (`app/` source), TypeScript
- TresJS 5 через `@tresjs/nuxt`, Three.js 0.186
- **Нет** Nuxt UI, Nuxt Content, Nuxt v5-nightly
- **Нет** pnpm (только npm), **нет** локального HTTPS
- Запуск: `npm run dev` → `http://localhost:3000/`
- Ветка: `dev`

`app/app.vue` — `TresCanvas window-size`, clear `#0b0b0c`, внутри `StashExperience`.

## Координаты и повороты

Three.js: Y вверх, камера смотрит −Z. Открытая стена (мы) — **+Z**. Дальняя стена — −Z. Окно — левая стена (−X). Правая стена — +X. Размеры в метрах. Позиция меша — **центр** бокса.

Экранное «вправо» / «влево» для yaw:

- вправо = **−Y**
- влево = **+Y**

## Как работать

Маленькие шаги, объяснения, не вываливать готовый уровень. Graybox = ящики/цилиндры, не CSG, не физика, не открытый мир, не квестовые маркеры. Примитивы — тестовый визуал, потом модели из Blender; **не полировать ящики**. Ховер и клик вешать на **группы-якоря**, чтобы потом подменить меш.

TresJS HMR часто падает, если добавить/снять `:rotation` у `TresGroup` / `TresMesh` (`Cannot assign to read only property 'rotation'`). Лечится **полным reload** `http://localhost:3000/`, не ждать HMR.

После правок UI/сцены проверять в браузере: поведение, не один скриншот.

## Комната сейчас

`StashExperience.vue`: width **6.2**, depth **3.4**, height **2.7**, wall **0.24**. Камера `[0, 1.7, depth/2+0.95]`, fov 56, look-at `[0, 1.15, 0]`. Свет: ambient 0.45, directional `[-6, 3.2, 0.4]` intensity 1.2 (из окна).

Проём окна в левой стене — четыре бокса вокруг дырки, не вырезание. `opening`: width 1.35, height 1.15, sill 1.05, z −0.15.

Файлы сцены:

- `app/components/StashWindow.vue` — рама, импост, форточка
- `app/components/StashDesk.vue` — стол-кластер
- `app/components/StashSideboard.vue` — сервант + прибор скилов
- `app/components/StashArmchair.vue` — кресло + КПК

### Окно

Рама тоньше стены в 2.5 раза, утоплена (`innerReveal` 0.05), виден откос. Стекло opacity 0.32. Снаружи одна серая плоскость. Форточка — `TresGroup` вокруг петли, `ventAngle = -0.22`, приоткрыта внутрь. Камень/трещины вокруг дырки — **потом**. Ветер форточки — потом.

### Стол (слева у дальней стены)

Graybox: `tableW` 1.85, `tableD` 1.12, `tableH` 0.84, `wallGap` 0.28. ЭЛТ слева, yaw **+14°**. Системник правее монитора с зазором 0.22. Клавиатура у ближнего края, yaw **+15°**. Стул yaw **−190°**. Лампа между системником и правым краем стола.

**Сейчас смотрим модель:** столешница — Sketchfab «Old Soviet table» (CC-BY-4.0 kayumov127), высота **0.92 м**, ширина ×**1.215**, yaw **+272°**. Лампа — Sketchfab «Rusty Kerosene Lamp» (CC-BY-4.0 GameDev Nick), **0.47 м**, к камере +9 см, покой 0.48 / ховер-фокус 2.2. Комп — Sketchfab «Abandoned Computer Terminal» (`public/models/abandoned-computer-terminal/`, CC-BY-4.0 olegolegperepelica): монитор+системник снизу+клава одним пропом, высота **0.52 м**, слева на столе, yaw **+14°**, сдвинут к дальней стене на **8 см** и вправо на **6.5 см**. Эмиссив экрана пульсирует с ховером стола. Стул — Sketchfab «Chair - Texture & Render» (`public/models/chair-texture-render/`, CC-BY-4.0 Rafiul Islam), высота **1.1 м**, yaw **−190°** (ещё −160° вправо от −30°), текстуры 512 AVIF. Бумаги — Sketchfab «Papers & Envelopes» (`public/models/papers-envelopes/`, CC-BY-4.0 bossdeff), между компом и лампой, +12 см вправо от середины, пролёт **0.65 м**, yaw **−21°**, 512 AVIF.

### Сервант (дальняя стена, справа, зазор 0.5 м до правой стены)

Yaw **−7°**. Низ `cabH` 0.92, `bodyD` = 1.5 × глубины полок (`hutchD` 0.52 → 0.78). Верх — открытый короб, две пустые полки. Прибор скилов на крышке нижнего ящика: **левый** край, чуть к полкам (не свешивается к камере), yaw **−24°**. Оливка, ручка, штырь, янтарный экран. СВД — Sketchfab «Dragunov SVD» (`public/models/dragunov-svd/`, CC-BY-4.0 Vintar), длина **1.6 м** (масштаб стола, не натура 1.22), слева от серванта **−50 см**, от стены к камере **+39 см** от спинки, над полом **8 см**, yaw **−121°** (+64° вокруг ствола), ствол к дальней стене и к левому боку серванта (наклон X **−8°**, Z **−15°**), 512 AVIF.

### Кресло (правая стена, ближе к камере)

Быльца до пола, выступают вперёд на 7 см. От стены `wallGap` **0.43**. Yaw `π/2 − 18°` (сиденье в комнату, вправо). `originZ = innerFrontZ - 0.72` — в кадре чуть меньше половины, спинка режется. КПК **стоит** на дальнем быльце, зелёный экран в комнату. Не путать с рабочим стулом.

## План

Сделано: сетап Nuxt/TresJS, graybox комнаты, окно-проём+рама, стол, сервант, кресло, прибор скилов, КПК, **ховер якорей**.

Ховер: `app/composables/useStashAnchor.ts`. Площадь — **весь кластер** (стол / сервант / кресло), не мелкий предмет. Курсор pointer, светится якорь внутри: CRT / янтарный прибор / КПК. Невидимый объём чуть больше мебели, без лишнего воздуха по краям (у `TresGroup` нет raycast).

Камера: `useStashCamera.ts` + `useStashLookaround.ts`. Кадр 0 живой (yaw ±1.5–2.5°), глаза **1.7 м**. Одна походка на все подходы, число шагов от дистанции: стол **2** ~1.18 с, сервант **3** ~1.77 с, кресло **2** ~1.18 с. Эллиптические дуги (бок 7.8 см, вверх 9.5 см), Y не падает. Клик по **столу** → шаги → **посадка** ~1.05 с (smootherstep) в плоскость ЭЛТ (0.7 м, стекло **1.33 м**, системник поднимает монитор) → **вплотную** ~0.48 с (**0.17 м**, ЭЛТ ~70% кадра, камера на **5 см** выше центра стекла). Клик по **серванту** → 3 шага к левому ближнему углу (стоп ~0.58 м от угла): первые два взгляд чуть на корпус, третий замечаем прибор → короткая пауза стоя (~0.4 с) → прибор поднимается в руки ~0.9 с (центр, крупно, как телефон). Клик по **креслу** → 2 шага к сиденью, взгляд на КПК (не разворачиваемся на кресло) → посадка ~1.05 с: камера влево и вниз одновременно → КПК поднимается в том же взгляде ~0.9 с (камеру не крутим). Назад — только кнопка **Назад**: со стола встать ~0.82 с затем шаг домой; с серванта положить прибор, затем шаг домой; с кресла положить КПК, встать, шаг домой. Пока якорь в фокусе, его экран пульсирует (`stashFocused`).

**Сейчас все три якоря сделаны.** Посадку/чтение ЭЛТ не шлифуем, пока не будет инфы на экране.

Дальше по шагам: декорации (модели из Загрузок) → 512 AVIF → расстановка. Хозяин ищет модель и говорит куда, агент жмёт и вставляет. Экран загрузки — потом. Оверлей / имя на стене — когда скажет.

Потом / не сейчас: шлифовка камер после контента, мобильный редирект, пост, камень окна, ветер форточки, стафф на полках.

## Ассеты / нагрузка

Тормоз не полигоны (стол ~800, лампа ~12k, комп ~30k) и не «невидимые стенки». Sketchfab кладёт **8K/4K**. Один metallic PNG компа был **47 МБ / 8192²** — декод PNG на главном потоке и заливка в GPU. Параллельный fetch уже есть, он не спасает декод.

Правило: в `public/models` только **512 AVIF** (`*.512.avif`, `EXT_texture_avif`). Скрипт `scripts/compress-model-textures.mjs 512 avif --in <src> --out <dest>` (нужен `sharp`). Сырой дамп из Загрузок в репу не класть. Резать грани / Draco — потом, если меши станут тяжёлыми.

## Сознательно не делаем

Физика, открытый мир, WASD, квестовые маркеры, Nuxt UI/Content, pnpm, локальный HTTPS, полировка примитивов «под финал».
